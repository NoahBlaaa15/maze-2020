#maze 2020 path module


def calculate_path(task, map, coords):

    if task == 'ramp_up':
        return task
    elif task == 'ramp_down':
        return task

    # todo loop through every room
        # todo give every room a cost
        # todo same layer is normal cost
        # todo different layer is triple cost

    # todo do a* until we have target at top of queue

    return path
